// Code generated by ent, DO NOT EDIT.
package ent

{{ range $n := $.Nodes }}
    {{ $builder := $n.CreateName }}
    {{ $receiver := receiver $builder }}
    {{ $up := $n.UpdateOneName}}


    func ({{ $receiver }} *{{ $builder }}) Set{{ $n.Name }}(input *{{ $n.Name }}) *{{ $builder }} {
        {{- range $f := $n.Fields }}
            {{- if eq $f.StructField "CreatedAt" "UpdatedAt" "DeletedAt"}}
            {{- else}}
                {{- $setter := print "Set" $f.StructField }}
                {{ $receiver }}.{{ $setter }}(input.{{ $f.StructField }})
            {{- end }}
        {{- end }}
        return {{ $receiver }}
    }

    func ({{ $receiver }} *{{ $up }}) Set{{ $n.Name }}(input *{{ $n.Name }}) *{{ $up }} {
        {{- range $f := $n.Fields }}
            {{- if eq $f.StructField "CreatedAt" "UpdatedAt" "DeletedAt"}}
            {{- else}}
                {{- $setter := print "Set" $f.StructField }}
                {{ $receiver }}.{{ $setter }}(input.{{ $f.StructField }})
            {{- end }}
        {{- end }}
        return {{ $receiver }}
    }
{{ end }}